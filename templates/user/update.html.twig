<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Update</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
{#    <link rel="stylesheet" href="styles.css">#}
</head>
<style>
    /* Basic Reset */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    /* Body and Background */
    body {
        font-family: 'Poppins', sans-serif;
        background: linear-gradient(to right, #00aaff, #87cefa); /* Sky blue gradient */
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        color: #333;
    }

    /* Container */
    .container {
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        align-items: center;
    }

    /* Header */
    .header {
        width: 100%;
        background-color: #ffffff;
        padding: 20px 40px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        margin-bottom: 40px;
        border-radius: 10px;
    }

    .user-info {
        font-size: 18px;
        font-weight: 600;
        color: #00aaff;
    }

    .header-buttons {
        display: flex;
        gap: 15px;
    }

    .header-buttons .btn {
        background-color: #00aaff;
        border: none;
        padding: 12px 20px;
        font-size: 16px;
        font-weight: 600;
        color: #fff;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .header-buttons .btn:hover {
        background-color: #0099cc;
    }

    /* Form Styling */
    .form-container {
        background: #fff;
        padding: 40px 50px;
        width: 100%;
        max-width: 500px;
        text-align: center;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        border-radius: 15px;
        animation: fadeIn 1s ease-out;
    }

    h1 {
        margin-bottom: 30px;
        font-size: 24px;
        font-weight: 600;
        color: #333;
        text-transform: uppercase;
    }

    /* Input Fields */
    .input-group {
        margin-bottom: 20px;
        position: relative;
    }

    .input-group label {
        font-weight: 600;
        margin-bottom: 8px;
        display: block;
        opacity: 0.7;
        color: #333;
    }

    .input-group input {
        width: 100%;
        padding: 12px 15px;
        font-size: 16px;
        background-color: #f2f9ff;
        border: 1px solid #d9e9ff;
        border-radius: 5px;
        color: #333;
        outline: none;
        transition: all 0.3s ease;
    }

    .input-group input:focus {
        background-color: #e6f4ff;
        box-shadow: 0 0 10px rgba(0, 170, 255, 0.6);
        transform: translateY(-2px);
    }

    /* Button Styling */
    button {
        width: 100%;
        padding: 15px;
        font-size: 18px;
        font-weight: 600;
        background-color: #00aaff;
        border: none;
        border-radius: 5px;
        color: #fff;
        cursor: pointer;
        transition: transform 0.2s ease-in-out, background-color 0.3s;
    }

    button:hover {
        transform: scale(1.05);
        background-color: #0099cc;
    }

    /* Hover Animation */
    @keyframes fadeIn {
        0% {
            opacity: 0;
            transform: translateY(-50px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

</style>
<body>
    <div class="container">
        <div class="header">
            <div class="user-info">
                <p>Welcome, <span id="userName">{{ user }}</span></p>
            </div>
            <div class="header-buttons">
                <button onclick="window.location.href='/home'" class="btn">Home</button>
                <button onclick="logout()" class="btn">Logout</button>
            </div>
        </div>

        <div class="form-container">
            <h1>Update Your Profile</h1>
            <form action="{{ path('update_profile_post') }}" method="POST">
                <div class="input-group">
                    <label for="firstName">First Name</label>
                    <input type="text" value="{{ currentUser.lastName }}" id="firstName" name="firstName" placeholder="Enter your first name">
                </div>
                <div class="input-group">
                    <label for="lastName">Last Name</label>
                    <input type="text" value="{{ currentUser.firstName }}" id="lastName" name="lastName" placeholder="Enter your last name">
                </div>
                <div class="input-group">
                    <label for="email">Email</label>
                    <input type="email" value="{{ currentUser.email }}" id="email" name="email" placeholder="Enter your email">
                </div>
                <div class="input-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Enter new password">
                </div>
                <button type="submit">Update Profile</button>
            </form>
        </div>
    </div>

    <script>
        // Simulated logout function (you can replace with actual logic)
        function logout() {
            window.location.href = '/logout'; // Redirect to login page after logout
        }
    </script>
</body>
</html>
